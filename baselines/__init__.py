"""Baseline policies for the Blindspot environment.

Each baseline implements `run_episode(env, user_id, seed) -> RewardBreakdown-like dict`
and can be run via `python -m baselines.<name>` or imported from
notebooks/01_demo.ipynb to produce the comparison plot.
"""
